## Summary

- What changed and why (1–3 sentences).
- How to test (bot / `npm start`, PHP `/api/health.php`, etc.).

## Checklist

- [ ] No secrets: `.env`, `site.config.php`, `data/*.db`, `local-root.php` are **not** committed.
- [ ] Docs updated if behaviour or install steps changed (`README.md` or `DEPLOY.md`).
